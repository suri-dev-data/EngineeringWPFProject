<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <SolidColorBrush x:Key="Win98FaceBrush" Color="#D4D0C8"/>
    <SolidColorBrush x:Key="Win98ShadowBrush" Color="Black"/>
    <SolidColorBrush x:Key="Win98MediumShadowBrush" Color="#808080"/>
    <SolidColorBrush x:Key="Win98HighlightBrush" Color="White"/>
    <SolidColorBrush x:Key="Win98MediumHighlightBrush" Color="#F4F4F4"/>

    <SolidColorBrush x:Key="Win98TextBrush" Color="Black"/>
    <SolidColorBrush x:Key="Win98InputBackgroundBrush" Color="White"/>
    <SolidColorBrush x:Key="Win98SelectedBackgroundBrush" Color="#0A246A"/>
    <SolidColorBrush x:Key="Win98SelectedTextBrush" Color="White"/>

    <Style x:Key="Win98LabelStyle" TargetType="Label">
        <Setter Property="Foreground" Value="{StaticResource Win98TextBrush}"/>
        <Setter Property="FontFamily" Value="MS Sans Serif"/>
        <Setter Property="Margin" Value="0,5,0,2"/>
    </Style>

    <Style x:Key="Win98ComboBoxToggleButton" TargetType="ToggleButton">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Grid>
                        <Border x:Name="Border" 
                                Background="{StaticResource Win98FaceBrush}"
                                BorderBrush="{StaticResource Win98MediumShadowBrush}" 
                                BorderThickness="1,1,0,0"/>
                        <Border BorderBrush="{StaticResource Win98ShadowBrush}" 
                                BorderThickness="0,0,1,1" Margin="0"/>

                        <Path x:Name="Arrow" 
                              Fill="Black" 
                              HorizontalAlignment="Center" 
                              VerticalAlignment="Center" 
                              Data="M 0 0 L 4 4 L 8 0 Z"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsPressed" Value="true">
                            <Setter TargetName="Border" Property="Background" Value="#C0C0C0"/>
                            <Setter TargetName="Arrow" Property="Margin" Value="1,1,0,0"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBox">
        <Setter Property="SnapsToDevicePixels" Value="true"/>
        <Setter Property="OverridesDefaultStyle" Value="true"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="ScrollViewer.CanContentScroll" Value="true"/>
        <Setter Property="FontFamily" Value="MS Sans Serif"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <Border BorderBrush="{StaticResource Win98MediumShadowBrush}" BorderThickness="1,1,0,0"/>
                        <Border BorderBrush="{StaticResource Win98HighlightBrush}" BorderThickness="0,0,1,1"/>

                        <Border BorderBrush="{StaticResource Win98ShadowBrush}" BorderThickness="1,1,0,0" Margin="1">
                            <Border BorderBrush="{StaticResource Win98MediumHighlightBrush}" BorderThickness="0,0,1,1" Background="{StaticResource Win98InputBackgroundBrush}">
                                <Grid>
                                    <ToggleButton 
                                        Name="ToggleButton" 
                                        Style="{StaticResource Win98ComboBoxToggleButton}" 
                                        Focusable="false"
                                        HorizontalAlignment="Right"
                                        IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" 
                                        ClickMode="Press"/>

                                    <ContentPresenter 
                                        Name="ContentSite" 
                                        IsHitTestVisible="False" 
                                        Content="{TemplateBinding SelectionBoxItem}"
                                        ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                        ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                        Margin="3,1,21,1"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Left" />

                                    <Popup 
                                        Name="Popup"
                                        Placement="Bottom"
                                        IsOpen="{TemplateBinding IsDropDownOpen}"
                                        AllowsTransparency="True" 
                                        Focusable="False"
                                        PopupAnimation="Slide">
                                        <Grid 
                                            Name="DropDown"
                                            SnapsToDevicePixels="True"                
                                            MinWidth="{TemplateBinding ActualWidth}"
                                            MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                            <Border 
                                                x:Name="DropDownBorder"
                                                Background="{StaticResource Win98InputBackgroundBrush}"
                                                BorderThickness="1"
                                                BorderBrush="Black"/>
                                            <ScrollViewer Margin="1" SnapsToDevicePixels="True">
                                                <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                            </ScrollViewer>
                                        </Grid>
                                    </Popup>
                                </Grid>
                            </Border>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBoxItem">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="true">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Win98SelectedBackgroundBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource Win98SelectedTextBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <Style x:Key="RoundToggleButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource Win98FaceBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource Win98TextBrush}"/>
        <Setter Property="FontFamily" Value="MS Sans Serif"/>
        
        <Setter Property="Padding" Value="8,2"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid x:Name="ButtonGrid">
                        <Border x:Name="OuterBorder" 
                                Background="{StaticResource Win98FaceBrush}"
                                BorderBrush="{StaticResource Win98ShadowBrush}" 
                                BorderThickness="1"
                                CornerRadius="5">

                            <Border x:Name="InnerBorder"
                                    BorderBrush="{StaticResource Win98HighlightBrush}" 
                                    BorderThickness="1,1,0,0"
                                    CornerRadius="4"
                                    Padding="{TemplateBinding Padding}">

                                <ContentPresenter x:Name="Content" 
                                                  HorizontalAlignment="Center" 
                                                  VerticalAlignment="Center"/>
                            </Border>
                        </Border>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="InnerBorder" Property="Background" Value="#E0E0E0"/>
                        </Trigger>

                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="InnerBorder" Property="BorderThickness" Value="0,0,1,1"/>
                            <Setter TargetName="InnerBorder" Property="BorderBrush" Value="{StaticResource Win98MediumShadowBrush}"/>
                            <Setter TargetName="Content" Property="Margin" Value="1,1,0,0"/>
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource Win98MediumShadowBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="TextBox">
        <Setter Property="FontFamily" Value="MS Sans Serif"/>
        <Setter Property="Background" Value="{StaticResource Win98InputBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource Win98TextBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Grid>
                        <Border BorderBrush="{StaticResource Win98MediumShadowBrush}" BorderThickness="1,1,0,0"/>
                        <Border BorderBrush="{StaticResource Win98HighlightBrush}" BorderThickness="0,0,1,1"/>
                        <Border BorderBrush="{StaticResource Win98ShadowBrush}" BorderThickness="1,1,0,0" Margin="1">
                            <Border BorderBrush="{StaticResource Win98MediumHighlightBrush}" BorderThickness="0,0,1,1" Background="{TemplateBinding Background}">
                                <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                            </Border>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="Button">
        <Setter Property="Background" Value="#C0C0C0"/>
        <Setter Property="Foreground" Value="Black"/>
        <Setter Property="FontFamily" Value="MS Sans Serif"/>
        <Setter Property="Padding" Value="10,2"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="OuterBorder" BorderThickness="0" BorderBrush="Black">
                        <Border x:Name="Border" 
                            Background="{TemplateBinding Background}" 
                            BorderThickness="2"
                            SnapsToDevicePixels="True">
                            <ContentPresenter x:Name="Content"
                                          HorizontalAlignment="Center" 
                                          VerticalAlignment="Center" 
                                          Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsPressed" Value="False">
                            <Setter TargetName="Border" Property="BorderBrush" Value="#FFFFFF"/>
                        </Trigger>

                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="#FFFFFF"/>
                            <Setter TargetName="Content" Property="Margin" Value="11,3,9,1"/>
                        </Trigger>

                        <Trigger Property="IsDefault" Value="True">
                            <Setter TargetName="OuterBorder" Property="BorderThickness" Value="1"/>
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="#808080"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>



    <Style TargetType="DataGrid">
        <Setter Property="FontFamily" Value="MS Sans Serif"/>
    </Style>

    <Style TargetType="DataGridColumnHeader">
        <Setter Property="FontFamily" Value="MS Sans Serif"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>

    <Style TargetType="DataGridCell">
        <Setter Property="FontFamily" Value="MS Sans Serif"/>
    </Style>

    <Style TargetType="TextBlock">
        <Setter Property="FontFamily" Value="MS Sans Serif, Tahoma, MS Reference Sans Serif"/>
        <Setter Property="Foreground" Value="Black"/>

        <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
        <Setter Property="TextOptions.TextRenderingMode" Value="Aliased"/>

        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>

    <Style TargetType="Path">
        <Setter Property="Fill" Value="Black"/>
        <Setter Property="Stroke" Value="Gray"/>
        <Setter Property="StrokeThickness" Value="1"/>
    </Style>
    
    <Style TargetType="Border">
        <Setter Property="Background" Value="{StaticResource Win98MediumShadowBrush}"/>
        <Setter Property="BorderBrush" Value="Black"/>
        <Setter Property="RenderOptions.EdgeMode" Value="Aliased"/>
    </Style>

    <Style TargetType="ProgressBar">
        <Setter Property="Foreground" Value="#000080" />
        <Setter Property="Background" Value="#C0C0C0" />
        <Setter Property="Height" Value="20" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ProgressBar">
                    <Grid Name="TemplateRoot">
                        <Border BorderBrush="#808080" BorderThickness="1" Background="{TemplateBinding Background}">
                            <Border BorderBrush="#FFFFFF" BorderThickness="0,0,1,1">
                                <Border BorderBrush="#000000" BorderThickness="1,1,0,0">

                                    <Grid Name="PART_Track" Margin="2">
                                        <Decorator Name="PART_Indicator" HorizontalAlignment="Left">
                                            <Border Name="IndicatorBorder" Background="{TemplateBinding Foreground}">
                                                <Border.OpacityMask>
                                                    <VisualBrush TileMode="Tile" Viewport="0,0,8,10" ViewportUnits="Absolute">
                                                        <VisualBrush.Visual>
                                                            <Rectangle Fill="Black" Width="6" Height="10" Margin="0,0,2,0"/>
                                                        </VisualBrush.Visual>
                                                    </VisualBrush>
                                                </Border.OpacityMask>
                                            </Border>
                                        </Decorator>
                                    </Grid>

                                </Border>
                            </Border>
                        </Border>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="Value" Value="100">
                            <Setter TargetName="IndicatorBorder" Property="Background" Value="#000080" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Ellipse">
        <Setter Property="Fill" Value="#C0C0C0" />
        <Setter Property="StrokeThickness" Value="2" />
        <Setter Property="Stroke">
            <Setter.Value>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#FFFFFF" Offset="0.0" />
                    <GradientStop Color="#FFFFFF" Offset="0.4" />
                    <GradientStop Color="#808080" Offset="0.6" />
                    <GradientStop Color="#404040" Offset="1.0" />
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="0" 
                              ShadowDepth="1.5" 
                              Direction="315" 
                              Color="Black" 
                              Opacity="0.8"/>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>